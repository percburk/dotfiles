---
name: hunk
description: Load the Hunk skill and use it for this review.
---

Run `hunk skill path` to get the skill path, then load and use the included Hunk skill. If your shell commands are running in a sandboxed environment, you will need to run the command outside the sandbox to see running `hunk` daemon servers.
